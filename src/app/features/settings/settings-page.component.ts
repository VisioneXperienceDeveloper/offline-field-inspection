import {ChangeDetectionStrategy, Component, inject} from '@angular/core';import {FormsModule} from '@angular/forms';import {ConnectivityService} from '../../core/services/connectivity.service';import {ToastService} from '../../core/services/toast.service';
interface Preferences{defaultInspector:string;autoSync:boolean;wifiOnly:boolean;photoMetadata:boolean;compactRegister:boolean}
@Component({selector:'app-settings-page',standalone:true,imports:[FormsModule],template:`<main class="page"><header><h1>Settings</h1><p>Configure field data, offline behaviour and workspace preferences.</p></header><form (ngSubmit)="save()"><section><header><span>◎</span><div><h2>Field profile</h2><p>Defaults applied when a new inspection is created.</p></div></header><div class="fields"><label><span>Default inspector</span><input name="inspector" [(ngModel)]="prefs.defaultInspector"></label><label><span>Date and time format</span><select disabled><option>English (Australia)</option></select></label></div></section><section><header><span>↻</span><div><h2>Offline and sync</h2><p>Control how local changes are uploaded when connectivity returns.</p></div></header><label class="toggle"><div><strong>Automatic sync</strong><small>Upload pending changes as soon as a network is available.</small></div><input type="checkbox" name="autoSync" [(ngModel)]="prefs.autoSync"><i></i></label><label class="toggle"><div><strong>Sync on Wi-Fi only</strong><small>Avoid uploading photo evidence over mobile data.</small></div><input type="checkbox" name="wifiOnly" [(ngModel)]="prefs.wifiOnly"><i></i></label><div class="connection"><i [class.online]="connectivity.online()"></i><div><strong>{{connectivity.online()?'Connection available':'Offline test mode enabled'}}</strong><small>{{connectivity.online()?'Automatic sync can run now.':'Changes remain safely on this device.'}}</small></div><button type="button" (click)="connectivity.setSimulatedOffline(connectivity.online())">{{connectivity.online()?'Test offline mode':'Return online'}}</button></div></section><section><header><span>▣</span><div><h2>Evidence and display</h2><p>Choose what is stored with field records.</p></div></header><label class="toggle"><div><strong>Store photo metadata</strong><small>Keep capture time and approximate location with evidence.</small></div><input type="checkbox" name="photoMetadata" [(ngModel)]="prefs.photoMetadata"><i></i></label><label class="toggle"><div><strong>Compact inspection register</strong><small>Reduce row height to show more records on screen.</small></div><input type="checkbox" name="compactRegister" [(ngModel)]="prefs.compactRegister"><i></i></label></section><footer><button type="button" (click)="reset()">Restore defaults</button><button class="primary">Save settings</button></footer></form></main>`,styleUrl:'./settings-page.component.css',changeDetection:ChangeDetectionStrategy.OnPush})
export class SettingsPageComponent{readonly connectivity=inject(ConnectivityService);private readonly toast=inject(ToastService);prefs:Preferences=this.load();save(){localStorage.setItem('fieldnote-preferences',JSON.stringify(this.prefs));this.toast.show('Settings saved.','success');}reset(){this.prefs=this.defaults();this.toast.show('Default settings restored. Save to apply them.','info');}private defaults():Preferences{return{defaultInspector:'Henry Kim',autoSync:true,wifiOnly:false,photoMetadata:true,compactRegister:false};}private load():Preferences{try{return{...this.defaults(),...JSON.parse(localStorage.getItem('fieldnote-preferences')??'{}')}}catch{return this.defaults();}}}
